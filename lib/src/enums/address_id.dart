/// Identifies [Address] in a [RouteSearchBox] widget.
enum AddressId {
  /// Identify [Address] object.
  origin,

  /// Identify [Address] object.
  destination,

  /// Identify [List] of [Address].
  waypoint,
}
