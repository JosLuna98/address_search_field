name: example
description: A flutter application for example of address_search_text_field plugin.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  toast: ^0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  address_search_text_field:
    path: ../

flutter:
  uses-material-design: true
